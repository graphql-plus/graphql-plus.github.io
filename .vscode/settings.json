{
  "liveServer.settings.port": 5661,
  "cSpell.words": ["extn", "gqlp", "POSIX", "Prnt", "regexes", "Scal"],
  "explorer.fileNesting.enabled": true,
  "explorer.fileNesting.expand": false,
  "explorer.fileNesting.patterns": {
    "*.g+req": "${capture}+*-errors,${basename}.*-errors,${basename}.resp",
    "*.gql+": "${capture}+*-errors,${basename}.*-errors",
    "*.graphql+": "${capture}+*-errors,${basename}.*-errors"
  }
}
